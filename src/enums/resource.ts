export enum ResourceType {
  Video = 'video',
  Audio = 'audio',
  Picture = 'picture',
  Sticker = 'sticker',
  Text = 'text',
  Effect = 'effect',
  Filter = 'filter',
}

export enum ResourceLoc {
  WrapTop = 1,
  Top,
}
